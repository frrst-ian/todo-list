class Project {
    constructor(name) {
        this.name = name;
        this.todos = [];
    }

    addTask(task) {
        this.todos.push(todo);
    }

    deleteTask(task) {
        this.todos.splice(index, 1);
    }
}

export default Project;